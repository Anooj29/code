# Generate a sequence of numbers
x <- 1:10
y <- x^2  # Square of each element in x

# Create a plot
plot(x, y, type = "b", col = "blue", 
     main = "Simple Plot Example",
     xlab = "X Values", ylab = "Y Values")
